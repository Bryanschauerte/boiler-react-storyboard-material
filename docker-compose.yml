version: '3.8'

services:

  client:
    # container_name: client
    stdin_open: true
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 3001:3000
    # environment:
    #   - CHOKIDAR_USEPOLLING=true